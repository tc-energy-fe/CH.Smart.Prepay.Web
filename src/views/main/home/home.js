import Actions from '@/store/actions-module'
import Mutations from '@/store/mutations-module'
// import * as types from '@/store/mutation-types'
// import api from '@/api/analysis'
// import apiUrl from '@/api/analysis/apiUrl'
// import download from '@/utils/download'
// import moment from 'moment'

const state = {}

const getters = {}

const actions = {
  ...Actions
}

const mutations = {
  ...Mutations
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
